# Evidence

Diagnostic scripts write timestamped evidence files here.

Generated `.txt` and `.log` files are ignored by Git because they can contain hostnames, IP configuration, interface details, and other environment-specific data. Review and sanitize any evidence before intentionally committing it.
